# SMTP for sending the form (nodemailer). Copy to .env.local and fill in.
# Gmail: enable 2FA and create an "App password" (https://myaccount.google.com/apppasswords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=maros.smrek@thunderstruck.studio
SMTP_PASS=heslo-aplikacie
# Where form submissions go, and the sender
CONTACT_TO=maros.smrek@thunderstruck.studio
CONTACT_FROM="Alma ADOS <maros.smrek@thunderstruck.studio>"

# Public site URL (for the OG image when sharing). On Vercel, VERCEL_URL is used otherwise.
NEXT_PUBLIC_SITE_URL=https://alma.sk
